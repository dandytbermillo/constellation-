/**
 * UI timing constants for consistent user experience
 */

/** Delay before showing preview on hover (milliseconds) */
export const PREVIEW_HOVER_DELAY_MS = 500;

/** Delay before hiding preview when mouse leaves (milliseconds) */
export const PREVIEW_CLOSE_DELAY_MS = 500;

/** Delay for toolbar button hover (milliseconds) */
export const TOOLBAR_HOVER_DELAY_MS = 300;

/** Duration of hover highlight animation (milliseconds) */
export const HOVER_HIGHLIGHT_DURATION_MS = 2000;
